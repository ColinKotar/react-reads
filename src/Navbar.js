import React from "react";
import styled from "styled-components";

const StyledNavbar = styled.div`
  width: 100%;
  min-height: 3rem;
  background-color: #3384ff;
`;

const Navbar = () => {
  return <StyledNavbar>React Reads</StyledNavbar>;
};

export default Navbar;
